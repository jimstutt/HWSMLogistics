# /bin/bash

# When executing the tree command to display only all user source code directory structure and file names; exclude  libraries like node_modules nor compiled results, data or .git files what  -I flags in tree -I flags should I use for this project?

